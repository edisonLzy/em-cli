# `@em-cli/em-plugin-build`

- [] js 库打包 (rollup)
- [] spa 项目 打包 (webpack)
- [] 组件库打包 (react,vue)
- [] 单文件打包 (.tsx,.vue)

## Usage

```
const emPluginBuild = require('@em-cli/em-plugin-build');

// TODO: DEMONSTRATE API
```

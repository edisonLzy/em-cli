import React,{FC} from 'react';
import Taro from '@tarojs/taro';
import { View, Button, Text } from '@tarojs/components';

import styles from  './index.less';

interface IProps{

}

const  <%= moduleName.replace(/^\S/, s => s.toUpperCase()) %>:FC<IProps> = (props)=>{
  return (
    <View className={styles.<%= moduleName %>}>
            this is <%= moduleName %>
    </View>
  );
}

export default <%= moduleName.replace(/^\S/, s => s.toUpperCase()) %>;

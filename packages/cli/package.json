{
  "name": "@em-cli/ee",
  "version": "0.0.0",
  "description": "make cli easier",
  "main": "lib/index.js",
  "bin": {
    "ee": "bin/ee"
  },
  "typings": "lib/typings",
  "files": [
    "lib",
    "bin"
  ],
  "scripts": {
    "clean": "rimraf lib",
    "start": "npm run clean && tsc --watch",
    "build": "npm run clean && tsc",
    "dev": "ts-node src/index.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@em-cli/core": "^1.4.10",
    "@em-cli/em-plugin-build": "^0.5.4",
    "@em-cli/em-plugin-create": "^0.3.0",
    "@em-cli/em-plugin-deploy": "^0.2.2",
    "@em-cli/em-plugin-dev": "^0.3.2",
    "@em-cli/em-plugin-init": "^0.5.0",
    "@em-cli/em-plugin-template": "^0.5.0"
  }
}

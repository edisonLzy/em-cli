export declare enum WriteFileModeEnum {
    /**
     * 输出文件已存在 覆盖
     */
    overwrite = "overwrite",
    /**
     * 输出文件已存在 会忽略继续输出后续的文件
     */
    skip = "skip",
    /**
     * 输出文件已存在 结束进程
     */
    error = "error"
}
